name: flyrank-capstone-widget-platform

run: npm start
seed: npm run migrate && npm run seed
test: npm test
base_url: http://localhost:3000

endpoints:
  health: /health
  widgets: /api/widgets
  widget_config: /api/public/widgets/:id/config
  submissions: /api/public/widgets/:id/submissions
  dashboard: /api/dashboard/stats
